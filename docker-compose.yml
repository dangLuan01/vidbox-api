services:
  api_watch:  
    build:
      dockerfile: Dockerfile
    container_name: api_watch
    #env_file: .env
    ports:
      - "8080:8080"
    environment:
      - VIRTUAL_HOST=watchapi.xoailac.top
      - LETSENCRYPT_HOST=watchapi.xoailac.top
      - LETSENCRYPT_EMAIL=ml2673572@gmail.com
      - VIRTUAL_PORT=8080
    networks:
      - app-network
    restart: unless-stopped
networks:
  app-network:
    external: true
    name: app-network